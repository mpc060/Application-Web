<%= form_with(model: colaboradore, local: true) do |form| %>
  <% if colaboradore.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(colaboradore.errors.count, "error") %> prohibited this colaboradore from being saved:</h2>

      <ul>
      <% colaboradore.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
